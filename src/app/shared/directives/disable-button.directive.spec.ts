import { DisableEvent } from './disable-button.directive';

describe('DisableButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableEvent(null);
    expect(directive).toBeTruthy();
  });
});
