import { DisableClickEventDirective } from './disable-click-event.directive';

describe('DisableClickEventDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableClickEventDirective();
    expect(directive).toBeTruthy();
  });
});
